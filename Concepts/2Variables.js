"use strict";    
/*The "use strict" directive enables JavaScript's strict mode. JavaScript's strict mode was introduced 
in ECMAScript 5. It enforces stricter parsing and error handling on the code at runtime. It also helps you write cleaner 
code and catch errors and bugs that might otherwise go unnoticed.
*/

// intro to variable

//variable can store some information
//we can use that information later
//we can change that information layter

//declare a variable
var firstName="Harshit"

//use a variable
console.log(firstName);

//change value
firstName="Mohit"
console.log(firstName);
